import { Download, Filter, Plus, Search } from "lucide-react";

export function ResourceTablePage({ eyebrow, title, description, action = "新建", columns, rows }: { eyebrow: string; title: string; description: string; action?: string; columns: string[]; rows: React.ReactNode[][] }) {
  return <div className="mx-auto max-w-[1440px] space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="eyebrow">{eyebrow}</p><h2 className="mt-1 text-xl font-bold">{title}</h2><p className="mt-1 text-[11px] text-[var(--muted)]">{description}</p></div><button className="inline-flex h-9 items-center justify-center gap-2 rounded-[4px] bg-[var(--brand)] px-4 text-[10px] font-semibold text-white"><Plus className="size-3.5" /> {action}</button></div><section className="panel overflow-hidden"><div className="flex flex-col gap-2 border-b border-[var(--line)] p-4 sm:flex-row"><label className="flex h-9 min-w-0 flex-1 items-center gap-2 border border-[var(--line)] px-3 sm:max-w-sm"><Search className="size-3.5 text-[var(--muted)]" /><input className="min-w-0 flex-1 text-[10px] outline-none" placeholder={`搜索${title}`} /></label><button className="inline-flex h-9 items-center justify-center gap-2 border border-[var(--line)] px-3 text-[10px]"><Filter className="size-3" /> 筛选</button><button className="inline-flex h-9 items-center justify-center gap-2 border border-[var(--line)] px-3 text-[10px]"><Download className="size-3" /> 导出</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-[10px]"><thead className="bg-[var(--surface-subtle)] text-[var(--muted)]"><tr>{columns.map((column) => <th key={column} className="px-5 py-3 font-medium">{column}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{rows.map((row, rowIndex) => <tr key={rowIndex} className="hover:bg-[var(--surface-subtle)]">{row.map((cell, cellIndex) => <td key={cellIndex} className="px-5 py-3.5">{cell}</td>)}</tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-[var(--line)] px-5 py-3 text-[9px] text-[var(--muted)]"><span>共 {rows.length} 条记录</span><span>每页 20 条</span></div></section></div>;
}
