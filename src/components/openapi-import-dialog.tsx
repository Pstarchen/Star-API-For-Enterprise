"use client";

import { FileJson2, Loader2, Upload, X } from "lucide-react";
import { type FormEvent, useState } from "react";
import type { CatalogProduct } from "@/lib/catalog";

const inputClass = "h-10 w-full rounded-[8px] border border-[var(--line)] bg-[var(--surface)] px-3 text-[10px] outline-none focus:border-[var(--brand)]";

export function OpenApiImportDialog({ defaultPublicHost, close, imported }: { defaultPublicHost: string; close: () => void; imported: (api: CatalogProduct, message: string) => void }) {
  const [billingMode, setBillingMode] = useState<"FREE" | "PER_REQUEST">("FREE");
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); const formElement = event.currentTarget; const form = new FormData(formElement); setSaving(true); setError("");
    const payload = new FormData(); payload.append("document", form.get("document") as File); payload.append("config", JSON.stringify({ name: String(form.get("name") ?? ""), slug: String(form.get("slug") ?? ""), publicHost: String(form.get("publicHost") ?? ""), publicPrefix: String(form.get("publicPrefix") ?? "/"), upstreamOverride: String(form.get("upstreamOverride") ?? ""), visibility: String(form.get("visibility") ?? "PUBLIC"), billingMode, unitPrice: billingMode === "FREE" ? 0 : String(form.get("unitPrice") ?? "0"), defaultQpsLimit: String(form.get("defaultQpsLimit") ?? "10") }));
    const response = await fetch("/api/v1/admin/apis/import", { method: "POST", body: payload }); const result = await response.json(); setSaving(false);
    if (!response.ok) { setError(result.message || "OpenAPI 导入失败"); return; } imported(result.data, result.message);
  }
  return <div className="fixed inset-0 z-50 overflow-y-auto bg-black/45 p-3 sm:p-5" onMouseDown={close}><form onSubmit={submit} onMouseDown={(event) => event.stopPropagation()} className="mx-auto my-6 w-full max-w-3xl overflow-hidden rounded-[8px] border border-[var(--line)] bg-[var(--surface)] shadow-2xl"><header className="flex items-center justify-between border-b border-[var(--line)] px-5 py-4"><div><h3 className="text-[14px] font-bold">导入 OpenAPI</h3><p className="mt-1 text-[9px] text-[var(--muted)]">从 OpenAPI 3.x JSON/YAML 自动生成路由、参数和响应 Schema。</p></div><button type="button" onClick={close} className="grid size-8 place-items-center rounded-[7px] hover:bg-[var(--surface-subtle)]" aria-label="关闭"><X className="size-4" /></button></header><div className="space-y-5 p-5"><label className="flex min-h-32 cursor-pointer flex-col items-center justify-center rounded-[8px] border border-dashed border-[var(--line-strong)] bg-[var(--surface-subtle)] p-5 text-center"><FileJson2 className="size-6 text-[var(--brand)]" /><strong className="mt-3 text-[11px]">选择 OpenAPI 文档</strong><span className="mt-1 text-[9px] text-[var(--muted)]">支持 JSON、YAML、YML，最大 2 MB，单次最多 100 个端点</span><input name="document" required type="file" accept=".json,.yaml,.yml,application/json,application/yaml,text/yaml" className="mt-3 max-w-full text-[9px]" /></label><div className="grid gap-4 sm:grid-cols-2"><Field label="API 名称"><input name="name" required className={inputClass} placeholder="例如：企业数据服务" /></Field><Field label="唯一标识"><input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" className={inputClass} placeholder="enterprise-data" /></Field><Field label="API 域名"><input name="publicHost" required defaultValue={defaultPublicHost} className={inputClass} /></Field><Field label="公开路径前缀"><input name="publicPrefix" required defaultValue="/" className={inputClass} placeholder="/enterprise" /></Field><Field label="覆盖上游地址" optional><input name="upstreamOverride" type="url" className={inputClass} placeholder="留空使用 servers[0].url" /></Field><Field label="可见范围"><select name="visibility" defaultValue="PUBLIC" className={inputClass}><option value="PUBLIC">公开市场</option><option value="PRIVATE">指定企业</option><option value="GRAY">灰度测试</option><option value="INTERNAL">内部网关</option></select></Field><Field label="默认 QPS"><input name="defaultQpsLimit" type="number" min="1" defaultValue="10" className={inputClass} /></Field><Field label="计费方式"><select value={billingMode} onChange={(event) => setBillingMode(event.target.value as "FREE" | "PER_REQUEST")} className={inputClass}><option value="FREE">免费</option><option value="PER_REQUEST">按成功请求</option></select></Field>{billingMode === "PER_REQUEST" && <Field label="单价（元/次）"><input name="unitPrice" required type="number" min="0.000001" step="0.000001" className={inputClass} /></Field>}</div>{error && <p role="alert" className="rounded-[8px] bg-[var(--danger-soft)] px-3 py-2.5 text-[10px] text-[var(--danger)]">{error}</p>}</div><footer className="flex justify-end gap-2 border-t border-[var(--line)] px-5 py-4"><button type="button" onClick={close} className="h-9 rounded-[8px] border border-[var(--line)] px-4 text-[10px]">取消</button><button disabled={saving} className="inline-flex h-9 items-center gap-2 rounded-[8px] bg-[var(--brand)] px-4 text-[10px] font-semibold text-white disabled:opacity-50">{saving ? <Loader2 className="size-3.5 animate-spin" /> : <Upload className="size-3.5" />}{saving ? "正在导入" : "导入为草稿"}</button></footer></form></div>;
}

function Field({ label, optional = false, children }: { label: string; optional?: boolean; children: React.ReactNode }) { return <label className="block"><span className="mb-1.5 flex gap-1 text-[9px] font-semibold">{label}{optional && <em className="not-italic font-normal text-[var(--muted)]">可选</em>}</span>{children}</label>; }
